<Window x:Class="SSHClient.App.PortBindingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:SSHClient.App.ViewModels"
        Title="Port Bindings" Height="220" Width="360" WindowStartupLocation="CenterOwner">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="HTTP Port" FontWeight="SemiBold"/>
        <TextBox Grid.Row="0" Margin="0,4,0,8" Text="{Binding HttpPort, Mode=TwoWay}"/>
        <TextBlock Grid.Row="1" Text="SOCKS Port" FontWeight="SemiBold"/>
        <TextBox Grid.Row="1" Margin="0,4,0,8" Text="{Binding SocksPort, Mode=TwoWay}"/>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Save" Command="{Binding SavePortsCommand}" Margin="0,0,6,0"/>
            <Button Content="Close" IsCancel="True" />
        </StackPanel>
    </Grid>
</Window>
