<Window x:Class="SSHClient.App.RulesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:SSHClient.App.ViewModels"
        xmlns:appModels="clr-namespace:SSHClient.App.Models"
        mc:Ignorable="d"
        Title="Rules" Height="400" Width="640" WindowStartupLocation="CenterOwner">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,8">
            <Button Content="Refresh" Command="{Binding RefreshCommand}" Margin="0,0,6,0"/>
            <Button Content="Add" Command="{Binding AddRuleCommand}" Margin="0,0,6,0"/>
            <Button Content="Delete" Command="{Binding DeleteRuleCommand}"/>
        </StackPanel>
        <DataGrid Grid.Row="1" ItemsSource="{Binding Rules}" SelectedItem="{Binding SelectedRule, Mode=TwoWay}" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="120" />
                <DataGridTextColumn Header="Pattern" Binding="{Binding Pattern, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="200" />
                <DataGridComboBoxColumn Header="Type" SelectedItemBinding="{Binding Type, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" ItemsSource="{x:Static vm:RuleTypes.Options}" Width="100" />
                <DataGridComboBoxColumn Header="Action" SelectedItemBinding="{Binding Action, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" ItemsSource="{x:Static appModels:RuleActionHelper.Values}" Width="100" />
                <DataGridTextColumn Header="Profile" Binding="{Binding Profile, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="100" />
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Content="Export" Command="{Binding ExportRulesCommand}" Margin="0,0,6,0"/>
            <Button Content="Import" Command="{Binding ImportRulesCommand}"/>
        </StackPanel>
    </Grid>
</Window>
